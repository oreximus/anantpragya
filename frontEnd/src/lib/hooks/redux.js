import { useDispatch, useSelector } from "react-redux";

// Simple JavaScript versions without TypeScript types
export const useAppDispatch = () => useDispatch();
export const useAppSelector = useSelector;
